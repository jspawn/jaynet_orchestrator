# Dependencies for the test.run harness (tools/test/runner.py).
# httpx and fastapi are already pulled in by requirements-web.txt; these add the
# test runner itself. Install into the venv that tools.test.python points at:
#
#   uv venv /srv/orchestrator/.venv          # if you don't have one yet
#   uv pip install -r requirements.txt -r requirements-web.txt -r requirements-test.txt
#
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27          # ASGITransport for in-process API tests (also in web reqs)
mypy>=1.10           # type checking (dev only — keep out of the prod venv)
ruff>=0.6            # lint (CI + local; config in ruff.toml)
