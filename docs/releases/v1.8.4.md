# v1.8.4 — screenshot refresh

2026-09-09 · Docs-only release, no code changes. All 27 console screenshots
(`screenshots/`) re-shot against the current GUI — they predated the whole
v1.7.3→v1.8.3 cycle:

- chat: mic button in the composer, current top bar
- Admin → Presets: vision/stt boot slots, whisper pill, cloud model table
- Admin → Processes: vision/stt cards, binaries panel with the visible
  implicit-default row and cpu/none pills
- Admin → Eval: cases/results/statistics/proposals/benchmark sub-views
- MCP as its own group, connectors, flags with mine buttons
- README hero (`chat-run.png` / `chat-hero.png`) redone on the current
  layout with a fresh demo run
