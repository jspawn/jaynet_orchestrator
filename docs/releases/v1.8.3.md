# v1.8.3 — documentation catch-up (vision/stt slots)

2026-09-09 · Docs-only release, no code changes. The v1.8.2 features are
now covered outside the release notes:

- **README** — the "Models as infrastructure" bullet names the vision/stt
  helper slots, `llm.call images=[...]`, `audio.transcribe` and the mic
  button.
- **docs/admin.md** — Presets: vision/stt boot slots, the `.bin` "whisper"
  pill in the HF downloader, proxy re-render + reload on preset/slot saves.
  Processes: the binaries panel with the visible implicit-default row and
  empty `device_env` for CPU builds.
- **docs/models.md** — new "Vision + speech-to-text helpers" section.
- **docs/llama-ops.md** — `WHISPER=on` in the knobs table, proxy re-render
  in the save step, and a correction: the old "alias + port must match a
  static entry in litellm.yaml" contract was outdated — the render
  generates local entries from the preset catalog.
- **docs/glossary.md / docs/model-placement.md** — vision/stt slot entries.
- **docs/playbook.md** — tool count corrected (117 tools / 38 namespaces),
  `llm.call` covers images, new perception paragraph in §2.1.
