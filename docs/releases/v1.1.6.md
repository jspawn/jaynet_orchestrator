# v1.1.6 — README credit for graphify

2026-08-22 · Docs-only patch: the README's references table now credits
[Graphify-Labs/graphify](https://github.com/Graphify-Labs/graphify) — the
Apache-2.0 engine (pip package `graphifyy`) behind the shipped graphify
plugin's `graph.*` tools — alongside the j-space suite. No code changes.

## Upgrade

Pull, restart, done — or skip it; nothing runtime-relevant.
