Eval cancel & benchmark-aware statistics, a console design refresh, lifespan migration, and repo docs for AI-assisted dev.

- **Eval suites and benchmarks can be cancelled** (Admin → Eval): Cancel stops the run after the case in flight finishes; the summary is marked cancelled.
- **Benchmark reps no longer wobble the statistics** — benchmark results are flagged, Statistics counts live runs by default, and a brain dropdown scopes every KPI/trend/flakiness number to one variant.
- **Design refresh across the console** — tool state as status dots with a gold running band, readable 118ch nerd measure with a shared glyph gutter, ctx fill bar, admin coral demoted to an accent stripe + ADMIN pill, account/admin share the app tokens. Nerd/chat-bubbles is now a labeled toggle (desktop header); mobile follows your stored default.
- **FastAPI lifespan migration** — deprecation warnings gone, no behavior change.
- **CONTEXT.md** — code-facing glossary for AI-assisted dev sessions.
- **LEARNING_GUIDE.md** corrected and extended against the current code.

Full details in [CHANGELOG.md](CHANGELOG.md). Install: see README quick start (`scripts/quickstart.sh` for a throwaway test, `scripts/setup.sh` for a permanent install).
