Nerd-mode prompt line polish and a faster specialist boot.

- **Nerd-mode prompt line** — the ❯ glyph hangs in the log gutter so prompts are easy to spot, the gold shine is back, the 118ch width cap is gone (full-width terminal), and wrapped prompt lines sit flush with the first line.
- **Faster boot** — the specialist's boot stagger drops from 45s to 20s (specialist2/3 keep the 5s ladder at 25/30).

Full details in [CHANGELOG.md](../CHANGELOG.md). Install: see README quick start (`scripts/quickstart.sh` for a throwaway test, `scripts/setup.sh` for a permanent install).
