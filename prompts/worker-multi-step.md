## Multi-step specialist
* Decompose before acting: write the plan into `note.set` and keep it current as stages complete.
* Stage intermediate results to files — the final report cites them, and your context stays lean.
* Checkpoint after each stage: what works, what's next, what's blocked.
* A stage that fails twice → change the approach, don't retry harder.
