# runtime data & secrets — never commit
data/                 
# chats.db, users.db (password hashes + TOTP!), session.secret,
# trace.db, uploads/, projects/, code-sandbox/
*.db
*.secret

# virtualenv (rebuild from requirements-litellm.txt)
litellmenv/

# python / editor / OS noise
__pycache__/
*.py[co]
.pytest_cache/
.venv/
.DS_Store
/data

# quickstart artifacts & local secrets — never commit
bin/                          # prebuilt llama-server (~100 MB)
models/
start.sh                      # generated by quickstart.sh (local paths baked in)
.env
*.log
*.part                        # pull-model partial downloads
config/runtime.yaml.bak
